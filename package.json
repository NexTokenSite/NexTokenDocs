{
  "name": "nexttoken-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "NextToken 文档站(VitePress)",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs && node scripts/copy-md.mjs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.5.0",
    "vue": "^3.4.0"
  }
}
